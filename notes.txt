Folder str:
codesync/
├── client/                  # React + Vite
│   ├── src/
│   │   ├── components/
│   │   │   ├── Editor.jsx       # Monaco editor wrapper
│   │   │   ├── FileTree.jsx     # sidebar file tabs
│   │   │   ├── Chat.jsx         # per-room chat
│   │   │   ├── UserCursors.jsx  # live cursor overlays
│   │   │   └── Navbar.jsx
│   │   ├── pages/
│   │   │   ├── Home.jsx         # create/join room
│   │   │   ├── Room.jsx         # main editor page
│   │   │   └── Auth.jsx         # login / register
│   │   ├── socket.js            # socket.io client instance
│   │   ├── context/
│   │   │   └── AuthContext.jsx
│   │   └── App.jsx
│
├── server/
│   ├── models/                  # MongoDB schemas
│   │   ├── User.js
│   │   ├── Room.js
│   │   └── File.js
│   ├── routes/
│   │   ├── auth.js              # /api/auth/register, /login
│   │   └── rooms.js             # /api/rooms
│   ├── middleware/
│   │   └── authMiddleware.js    # verifies JWT
│   ├── socket/
│   │   └── socketHandlers.js    # all socket events live here
│   ├── db/
│   │   └── connect.js
│   ├── roomState.js             # in-memory Map for live sessions
│   └── index.js                 # entry point

1. Why Socket.io, not plain WebSockets?
Plain WebSockets give you a raw connection — no rooms, no reconnection logic, no fallback. Socket.io builds on top and gives you:
Rooms — socket.join(roomId) and io.to(roomId).emit(...) — broadcast only to users in that room, not everyone
Auto-reconnect — if the user's internet drops for 2 seconds, Socket.io reconnects and re-joins automatically
Fallback to HTTP long-polling — on networks that block WebSocket ports, it degrades gracefully
Namespaces — you can separate concerns (e.g. /editor vs /chat)
For real-time collaboration, plain WS would mean you'd rewrite all of this yourself.

2. Why Monaco Editor, not CodeMirror or a <textarea>?
Monaco is the exact engine that powers VS Code. It gives you for free: syntax highlighting for 50+ languages, bracket matching, Intellisense-style autocomplete, multiple cursors, and a clean onDidChangeModelContent event you can hook into. CodeMirror is also good but Monaco has better TypeScript support and looks more impressive in a demo.

3. The in-memory room state pattern
js// server/roomState.js
const rooms = new Map();
// rooms.get(roomId) = { code: "...", users: [], cursors: {} }
Why RAM and not MongoDB for live edits? Because a keystroke fires an event every 50-200ms. Writing to MongoDB on every keystroke would be 5-20 DB writes per second per user — extremely slow and expensive. Instead you keep the "hot" state in a Map on the server, and only flush to MongoDB when:
A user leaves the room
The room becomes empty
The user explicitly clicks "Save"
This is the same pattern Redis is used for in production apps — your RAM Map is your local Redis.

4. How code:change works (the core event)
js// client — fires on every editor change
editor.onDidChangeModelContent(() => {
  socket.emit("code:change", {
    roomId,
    code: editor.getValue()
  });
});

// server — receives and broadcasts to everyone else in room
socket.on("code:change", ({ roomId, code }) => {
  rooms.get(roomId).code = code; // update in-memory state
  socket.to(roomId).emit("code:update", { code }); // broadcast to others, NOT back to sender
});

// client — receives and applies (only runs on other users' screens)
socket.on("code:update", ({ code }) => {
  if (editor.getValue() !== code) {
    editor.setValue(code); // update editor without re-triggering the change event
  }
});
Note: socket.to(roomId) sends to everyone in the room except the sender. If you used io.to(roomId) it would echo back to the sender too, causing an infinite loop.

5. JWT in Socket.io — why it's different from HTTP
HTTP requests attach JWT in the Authorization header. But Socket.io is a persistent connection — there's no header after the handshake. So you validate JWT at the handshake moment:
js// server/index.js
io.use((socket, next) => {
  const token = socket.handshake.auth.token; // client sends this at connect time
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    socket.user = decoded; // attach user info to socket object
    next();
  } catch {
    next(new Error("Unauthorized"));
  }
});
js// client/socket.js
const socket = io(SERVER_URL, {
  auth: { token: localStorage.getItem("token") }
});
After this middleware runs, every socket event handler can trust socket.user is the authenticated user. No need to re-verify on every event.

6. Live cursors — how they work
Each user's cursor position is an { lineNumber, column } object from Monaco. You emit it on every cursor move and render other users' cursors as colored overlays:
js// client — emit cursor position
editor.onDidChangeCursorPosition(({ position }) => {
  socket.emit("cursor:move", { roomId, position, userId });
});

// server — relay to room (excluding sender)
socket.on("cursor:move", ({ roomId, position, userId }) => {
  socket.to(roomId).emit("cursor:update", { position, userId });
});
On the client you maintain a cursors state object like { userId: { position, color, username } } and render each one as an absolutely-positioned element over the editor.

7. MongoDB schemas — what to store
js// Room schema
{
  name: String,
  createdBy: ObjectId (ref User),
  files: [ObjectId (ref File)],
  members: [ObjectId (ref User)],
  isPrivate: Boolean,
  shareCode: String  // 6-char random code to join
}

// File schema
{
  room: ObjectId,
  name: String,       // "index.js"
  language: String,   // "javascript"
  content: String     // last saved code
}
The shareCode is important — it's how users join a room without needing to be invited. You generate a short random code (like "xk92pz") when the room is created, and anyone with that code can join.
“We store rooms and files separately in MongoDB to maintain clean relationships and scalability. The room schema manages metadata and access control, while the file schema stores the latest persisted code. Real-time edits are handled in memory, and only final states are saved.”


8. Why Vite and not Create React App?
CRA is deprecated and slow. Vite starts the dev server in under 300ms, has native ES module support, and handles environment variables cleanly. Always use Vite for new React projects in 2024+.
“Vite uses native ES modules to serve files on demand, resulting in near-instant startup and fast hot module replacement, whereas Create React App relies on Webpack bundling, making it slower and less efficient for modern development.”

The one thing that trips everyone up:
The echo loop. When user A types, the client emits code:change. The server updates state and emits code:update to everyone else. But if you accidentally emit back to the sender too (using io.to() instead of socket.to()), the sender receives their own update, which triggers another code:change, which triggers another broadcast — infinite loop, editor freezes. Always use socket.to(roomId) on the server to exclude the sender.
“An echo loop occurs when the server broadcasts updates back to the sender, causing the client to re-emit the same change repeatedly. This is avoided by using socket.to(roomId) to exclude the sender and adding client-side checks before applying updates.”

9. Agar koi aur website same port (e.g., http://localhost:5173) pe chal rahi ho, toh kya wo humare backend se data le sakti hai?
Case 1: Backend local machine par hai (not deployed)
localhost = same machine only
Dusra laptop ka localhost:5173 ≠ tumhara localhost:5173
So:
Dusra banda direct access nahi kar sakta
Jab tak:
usko tumhara IP address na mile (192.168.x.x)
aur wo usse connect na kare
✔️ Agar wo IP se connect kar leta hai + CORS allow karta hai → data mil jayega
Case 2: Backend LIVE hai (production) — ⚠️ important
Agar tumne CORS me ye likh diya:
origin: "http://localhost:5173"
 Then:
Koi bhi banda apne laptop pe localhost:5173 run karega
Aur tumhare live backend ko hit karega
Server bolega: “allowed hai”
Data leak ho jayega
✅ Solution
Use environment-based config:
origin: process.env.FRONTEND_URL
Dev → http://localhost:5173
Prod → https://your-real-site.com

10. Mongoose Schema Syntax (User.js)
Ek Schema Database mein collection (table) ka blueprint hota hai. Isko banate waqt class me 2 objects paas hote hain:

new mongoose.Schema( Object_1, Object_2 )

- Object_1 (BluePrint 🟢): Ye compulsory hota hai. Isme fields decide hote hain (username, email) aur unke rules (type: String, required: true, unique: true).
- Object_2 (Options 🟡): Ye optional hota hai. Isme settings aati hain, jaise {timestamps: true} jo automatically DB mein 'createdAt' aur 'updatedAt' column add kar deta hai.

mongoose.model("User", userSchema)
Sirf Schema (Blueprint) se database me kaam nahi hota. Model us blueprint ko factory me badalta hai taaki DB me data save/read kr sakein. 
Note: "User" naam dene se MongoDB apne aap usko database me lowercase aur plural  karke `users` collection bana deta hai.

/*
 * WORKFLOW SUMMARY (Mongoose & MongoDB):
 * 
 * 1. Schema Initialization (Runs Once): 
 *    The `mongoose.Schema()` function executes ONLY ONCE when the Node server starts. 
 *    It keeps this "blueprint" or "ruleset" in memory. Unlike SQL, it DOES NOT create 
 *    a pre-existing table with blank columns in the database.
 * 
 * 2. Document Creation (Runs per Request): 
 *    When a user creates a room, `Room.create(...)` (in routes/rooms.js) is called.
 *    The `roomSchema` function does NOT run again. Instead, Mongoose takes the incoming 
 *    data (name, user IDs, nanoid(6) shareCode) and validates it against the blueprint in memory.
 * 
 * 3. Database Insertion: 
 *    If the data strictly follows the blueprint rules, Mongoose packages it into a brand  
 *    new JSON-like object (a Document) along with the auto-generated timestamps.
 *    It then drops this newly created document right into the MongoDB collection.
 */

/*
 * WHY `room` IS REQUIRED IN FileSchema BUT `file` IS NOT REQUIRED IN RoomSchema?
 *
 * 1. The "Chicken-and-Egg" Conflict:
 *    If both schemas strictly required each other, neither could be created first. 
 *    You would need a Room ID to create a File, and a File ID to create a Room, 
 *    resulting in a system loop/deadlock.
 *
 * 2. Parent-Child Execution Flow:
 *    In a One-to-Many database relationship, the Parent (Room) must be created 
 *    first to generate a unique MongoDB ObjectId. Once the Room exists, the Child (File) 
 *    is created immediately after and attaches the Parent's ID to itself as a reference.
 *
 * 3. Preventing Orphan Data (Strictness):
 *    From a database perspective, a Room can safely exist without any code files inside it. 
 *    However, a Code File CANNOT exist in the system without belonging to a specific Room. 
 *    Therefore, `room: { required: true }` guarantees that no "orphan" files enter the DB.
 */

1. bcrypt salt rounds

bcrypt.hash(password, 10)

That 10 is basically:
“how hard do you want to make the hacker suffer?”

Internally it runs hashing 2¹⁰ = 1024 times
More rounds = slower hashing = harder to crack

Why it matters:

If someone steals your DB, they try brute force
Each guess becomes slow → attack becomes impractical

Reality check:

< 10 → weak, don’t do it
10–12 → sweet spot
> 14 → your login becomes a loading screen simulator

So yeah, you're trading security vs speed, not just picking a random number.

2. JWT payload is NOT encrypted

This is where people mess up badly.

JWT looks fancy, but:

It is encoded, NOT encrypted
Anyone can decode it on tools like jwt.io

So if you do this:

{ id, email, password }

Congrats, you just leaked user data publicly.

Correct mindset:
JWT = identity card, not a locker

Only put:

{ id, username }

Golden rule:
If you'd be uncomfortable showing it in frontend console → don’t put it in JWT.

3. socket.to() vs io.to()

This one is subtle but important.

io.to(room)
Sends message to everyone in room
INCLUDING sender
socket.to(room)
Sends to everyone except sender

Now think:

You're building a collaborative editor.

User types → emits change → server broadcasts

If you use:

io.to(room).emit("change")

Sender also gets it → applies change again → duplicate / loop / glitch

So we use:

socket.to(room).emit("change")

Translation:

io.to() = announcement
socket.to() = gossip behind your back

For live updates → always exclude sender.

4. In-memory vs DB

This is architecture thinking—good devs get this, average ones ignore it.

RAM (your Map)
Fast
Temporary
Used for live stuff

Example:

current code in editor
active users in room
MongoDB
Slow(er)
Permanent
Used for storage

Example:

saved files
user accounts

Flow:

While editing → keep in RAM
When user leaves / clicks save → push to DB

Why not DB directly?
Because hitting DB on every keystroke = suicide for performance.

5. router.use(protect)

This is just being lazy in a smart way.

Instead of:

router.get("/a", protect, handler)
router.get("/b", protect, handler)
router.get("/c", protect, handler)

You do:

router.use(protect)

router.get("/a", handler)
router.get("/b", handler)
router.get("/c", handler)

Now EVERYTHING below is protected.

Think of it like:

You put a security guard at the building entrance
Instead of checking ID at every room